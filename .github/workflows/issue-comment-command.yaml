name: Handle theme command

on:
  issue_comment:
    types: [created]

env:
  PYTHON_VERSION: '3.14'

permissions:
  contents: write
  issues: write

jobs:
  process-command:
    if: >-
      github.event.issue.pull_request == null &&
      github.event.comment.user.login == github.repository_owner &&
      startsWith(github.event.comment.body, '/theme ')
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Parse /theme command
        id: parse
        env:
          ISSUE_COMMENT_BODY: ${{ github.event.comment.body }}
          ISSUE_BODY: ${{ github.event.issue.body }}
          COMPATIBILITY_ISSUE: ${{ contains(github.event.issue.labels.*.name, 'theme-compatibility') }}
        run: |
          python .github/scripts/handle-theme-command.py

      - name: Rebuild README
        run: |
          python .github/scripts/generate-readme.py

      - name: Commit and push changes
        env:
          ACTION: ${{ fromJSON(steps.parse.outputs.result_json).action }}
          THEME_NAME: ${{ fromJSON(steps.parse.outputs.result_json).theme_name }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add themes.json README.md
          if git diff --cached --quiet; then
            echo "No metadata changes to push."
          else
            ACTION_LABEL="$(printf '%s' "$ACTION" | awk '{print toupper(substr($0,1,1)) substr($0,2)}')"
            git commit -m "${ACTION_LABEL} theme: ${THEME_NAME}"
            git push
          fi

      - name: Comment result
        uses: peter-evans/create-or-update-comment@v5
        with:
          issue-number: ${{ github.event.issue.number }}
          body: ${{ fromJSON(steps.parse.outputs.result_json).message }}

      - name: Close issue
        if: fromJSON(steps.parse.outputs.result_json).close_reason != ''
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          CLOSE_REASON: ${{ fromJSON(steps.parse.outputs.result_json).close_reason }}
        run: |
          gh issue close ${{ github.event.issue.number }} --reason "$CLOSE_REASON"
